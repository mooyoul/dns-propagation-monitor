const angular = require('angular');

angular.module('dns.propagation.monitor.component.filter', []);

require('./date-format');

module.exports = exports = 'dns.propagation.monitor.component.filter';
