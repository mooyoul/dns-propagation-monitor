const angular = require('angular');

angular.module('dns.propagation.monitor.component', [
  require('./filters'),
  require('./resources')
]);

module.exports = exports = 'dns.propagation.monitor.component';