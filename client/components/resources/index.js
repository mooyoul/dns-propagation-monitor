const angular = require('angular');

angular.module('dns.propagation.monitor.component.resource', [
  require('angular-resource')
]);

require('./lookup');

module.exports = exports = 'dns.propagation.monitor.component.resource';
